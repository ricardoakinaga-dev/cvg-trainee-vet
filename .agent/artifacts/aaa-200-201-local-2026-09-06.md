# AAA-200/201 — evidência local bounded

## Escopo

Contrato vertical diagnóstico B-07 → resultado → assignment → atividade/next
action. Evidência exclusivamente sintética/local; não é prova de G2, produção,
clínica, piloto ou competência prática.

## Execução

- comando focal: `corepack pnpm exec vitest run --project unit packages/application/src/adaptive-assignment-use-cases.test.ts packages/persistence/src/adaptive-assignment-repository.test.ts packages/persistence/src/diagnostic-session-repository.test.ts packages/contracts/src/diagnostic-session.test.ts packages/contracts/src/learning-state.test.ts`
- resultado focal inicial: `5` arquivos / `23` testes PASS;
- reteste pós-crítica: `5` arquivos / `25` testes PASS, incluindo resumo
  allowlisted sem IDs/módulos internos, source diagnostic divergente e vínculo
  de atividade divergente em falha fechada;
- regra verificada: `M01`, `M02`, `M11` obrigatórios + recomendações válidas,
  ordem curricular e deduplicação;
- integridade verificada: resultado persistido → identidade server-side,
  unicidade participante/escopo/módulo, replay com proveniência, promoção
  `NAO_ATRIBUIDO` com CAS e vínculo de atividade publicada com proveniência;
- gate global corrente: `corepack pnpm verify` PASS, `149` arquivos/`800`
  testes, `30` arquivos/`42` skipped, cobertura
  `84,45%/80,18%/87,30%/85,19%`;
- `corepack pnpm test:integration:live`: bloqueado no preflight pela ausência
  de `CVG_TEST_DATABASE_URL`.

## Limitações

Ainda faltam revisão independente pós-correção adequada, PostgreSQL/RLS live,
concorrência real, rollback, browser → web → API → PostgreSQL, grants/owners
produtivos, `AAA-202`, feedback/debrief, retenção, publicação clínica,
produção, piloto e release.
